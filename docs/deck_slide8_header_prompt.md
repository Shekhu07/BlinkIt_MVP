# Design Labs prompt — slide 8 header swap

Target file: `deck/design/Blinkit Category Nudge Case Study.dc.html`
Target element: the `<h2>` on **slide 8** (`data-screen-label="08"`, `data-label="The MVP, running"`).

Current text: "BlinkIQ: a nudge with a reason attached"
New text: **"BlinkIQ decides, nudges, and explains itself — live"**

---

## Prompt to paste into Design Labs

```
On slide 8 ("The MVP, running") of the Blinkit Category Nudge Case Study deck, replace
the h2 headline text with:

"BlinkIQ decides, nudges, and explains itself — live"

Do not change the font (Plus Jakarta Sans, 800 weight), size (48px), max-width (1150px),
line-height, letter-spacing, colour, or position. Do not touch any other element on this
slide or any other slide — the eyebrow ("Part 4 · the MVP, deployed"), the "Open the live
agent →" link, and the "Data disclosure" card all stay exactly as they are.

Confirm the new line doesn't wrap past 2 lines at 48px within the 1150px column, and that
it still reads clearly next to the adjacent "Open the live agent →" link (the word "live"
appears in both — check it doesn't feel redundant side by side; if it does, suggest a
minor rebalancing of spacing only, not a copy change).
```
