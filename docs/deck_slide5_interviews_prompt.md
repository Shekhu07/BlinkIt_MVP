# Design Labs prompt — slide 5: reflect the real interviews now in progress

Target file: `deck/design/Blinkit Category Nudge Case Study.dc.html`
Target slide: **slide 5** (`data-screen-label="05"`, `data-label="Research: confirmed & challenged"`)

## Why this slide, specifically
Slide 5's "Sample, stated honestly" card currently says: *"31 survey responses. No live
interviews were conducted — a deliberate trade of depth for breadth against a fixed
deadline."* That statement is now factually out of date — 5 real interview responses have
come in since. This prompt has two parts: **(A) a required accuracy fix**, and **(B) an
optional content addition** if you also want the new assortment/variety finding included.
Do only (A), or (A)+(B) — your call in Design Labs.

## What NOT to touch
- Do not recompute, relabel, or merge anything into the **N=31 survey figures** (58%, 48%,
  73%, the 11/9/11 split, the "confirmed/challenged" cards, the verdict band). Those stay
  frozen and sourced to the survey only — the interviews are a separate instrument with
  their own small sample and must not be blended into any survey percentage.
- Don't blow the slide's density — it's already a tight 3-column grid with no free
  vertical space. Any addition must be short (1-2 lines) and only in the "Sample, stated
  honestly" card or as a compact addition to the existing white "Confirmed, in their
  words" panel — do not add a new card/column.

---

## Prompt to paste into Design Labs

```
On slide 5 ("Research: confirmed & challenged"), part A (required): update the "Sample,
stated honestly" card (top right, currently: "31 survey responses. No live interviews
were conducted — a deliberate trade of depth for breadth against a fixed deadline. Six
respondents consented to follow-up calls that were not run.") to reflect that live
interviews are now underway. Replace with wording close to:

"31 survey responses, plus 5 live interviews now underway with consenting respondents
from that same sample — early qualitative signal, not yet a large enough set to report
as a finding on its own."

Keep the card's exact size, font (19px/800 weight label, 19px/600 weight body), padding,
and position — this is a text swap only, not a layout change.

Part B (optional — only if I ask for it separately): in the "Confirmed, in their words"
panel (bottom right, white card with three green-tinted quote blocks), you may add ONE
more compact quote block, same style as the existing three (EDF5E9 background, CFE3C6
border, italic quote + bold-green attribution line), using this real interview quote:

"Blinkit does not offer wide variety/options as compared to others"
— attribution: "Assortment gap, not trust (real interview)"

Only add this if it fits without the panel overflowing its column — if it would push the
panel past the other two columns' height, tell me instead of forcing it in, and suggest
trimming an existing quote rather than shrinking fonts below 19px.

Do not change any other slide, any N=31 number, or the "verdict" band at the bottom.
```
