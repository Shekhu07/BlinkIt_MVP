# Design Labs prompt — slide 5: fix header context + slim the verdict band

Target slide: **slide 5**, "Confirmed the cause. Challenged the fix." (Primary Research)

## What's wrong today
1. The top-right sample pill ("31 · THE SAMPLE — Survey responses, breadth over depth")
   and the speaker notes both still claim zero interviews. The slide body's "Interviews &
   quotes" panel currently shows "4 semi-structured interviews," but that count is itself
   wrong — 5 live interviews have actually been conducted. So there are two separate
   inconsistencies to fix: the header/notes need to say interviews happened at all, and
   the body panel's "4" needs to become "5."
2. The bottom yellow "The verdict" band is oversized for the single sentence it holds —
   wide icon gutters left and right, generous padding — and eats vertical space that could
   go toward context or content.

## What NOT to touch
- Don't change any N=31 survey number, any donut stat, or the "Asked directly" bar.
- Don't change the 3 existing representative quotes or their attributions.
- Don't change slide count, position in the deck, or any other slide.
- The correct interview count is **5**, not 4 — the panel's current "4 semi-structured
  interviews" label is itself wrong and must be corrected to 5, and the header must match
  that corrected number.

---

## Prompt to paste into Design Labs

```
On slide 5 ("Confirmed the cause. Challenged the fix."), make three changes:

1. HEADER + COUNT FIX (required): the top-right sample pill currently reads "31 · THE
   SAMPLE — Survey responses, breadth over depth," and the speaker notes say "zero live
   interviews" — both are wrong, since 5 live interviews have been conducted. The
   "Interviews & quotes" panel body also currently says "4 semi-structured interviews" —
   that number is wrong too and must become 5. Replace the top-right pill with two
   smaller stat chips shown together: one for the survey sample, one for the interview
   sample — "31 · SURVEY RESPONSES" and "5 · LIVE INTERVIEWS" — same visual weight as the
   current pill, same top-right position, same black/yellow colour treatment. Update the
   "4 semi-structured interviews" label in the body panel to "5 semi-structured
   interviews." Update the speaker notes to match (mixed-methods sample: 31 survey + 5
   interviews, not zero interviews).

2. BRIDGING SUBHEAD (required): add one line under the H2 "Confirmed the cause.
   Challenged the fix." explaining why two research methods are shown together, e.g.
   "Survey breadth and interview depth were triangulated against each other — where they
   agree is the strongest signal." Match it to the deck's existing subhead styling used
   elsewhere (regular weight, muted colour, sits directly under a bold H2).

3. SLIM THE VERDICT BAND (required): the bottom yellow verdict band is oversized for one
   sentence — wide icon columns left/right and generous padding. Flatten it into a single
   compact row: checkmark icon inline directly before the bold lead-in text, drop the
   separate icon gutters, reduce vertical padding to match the tighter card padding used
   elsewhere in this deck (roughly 14-16px, not the current ~40-50px). The goal is to cut
   this band's height by at least a third without shrinking the type below 19px (14pt) —
   the deck's hard minimum type size.

Do not change any N=31 statistic, any donut percentage, the "Asked directly" bar, or any
of the 3 existing quote cards and their attributions. Do not add a 4th quote card unless
I ask for it separately. Show me the result before applying anything else.
```
