# Figma prompt: propagate the "Blink & Try It" name to slides 7 and 8

The Category Nudge Agent is now named "Blink & Try It" (decided 2026-08-03). This
name has already been added to the coded deck (`deck/design/Blinkit Category
Nudge Case Study.dc.html`) in 3 places. The Figma build needs the same 2 edits
to stay in sync — screenshots show it still reads "The Category Nudge Agent."

## Prompt to paste into Figma

```
On Slide 7 ("A trust-led nudge wins because it answers the blocker customers
actually named"), in the dark "CHOSEN SOLUTION" panel on the right: change the
headline from "The Category Nudge Agent" to "Blink & Try It" as the large bold
line, with "— the Category Nudge Agent" as a smaller, lighter-weight subtitle
directly after it on the same line or immediately below. Keep the rest of that
panel (the description paragraph and "Why not a discount" section) unchanged.
Do not rename the "Trust-led category nudge ← chosen" row in the left-hand
table — that's a generic option label, not the product name.

On Slide 8 ("The agent runs today: a shopper-facing nudge, its reasoning, and
the push queue behind it"), change the H2 title to start with "Blink & Try It
runs today:" instead of "The agent runs today:" — keep the rest of the title
text identical. Leave every other "the agent" reference on this slide alone
(the annotation callouts, image captions, speaker notes) — those are fine as
generic references now that the name is established elsewhere.
```

## After it renders

Check slide 7 specifically — the panel was already tight on space before this
edit, and images-2000x1301 alt text confirms the headline area has limited
width. Confirm "Blink & Try It — the Category Nudge Agent" doesn't wrap awkwardly
or crowd the paragraph below it.
